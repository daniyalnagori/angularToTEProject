import { EventEmitter } from '@angular/core';
export declare class SuiDimmer {
    classes: boolean;
    isClickable: boolean;
    isDimmed: boolean;
    isDimmedChange: EventEmitter<boolean>;
    private click();
}
export declare const SUI_DIMMER_DIRECTIVES: typeof SuiDimmer[];
