import { AfterContentInit } from '@angular/core';
import { SuiTabContent } from './tab-content';
import { SuiTab } from "./tab";
export declare class SuiTabset implements AfterContentInit {
    private _tabs;
    private _tabContents;
    private _loadedTabs;
    private _activeTab;
    ngAfterContentInit(): void;
    private loadTabs();
    private tabStateChanged(tab);
    private activateFirstTab();
    private activateClosestTab(tab);
}
export declare const SUI_TABS_DIRECTIVES: (typeof SuiTabContent | typeof SuiTab | typeof SuiTabset)[];
