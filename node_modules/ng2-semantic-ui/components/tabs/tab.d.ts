import { EventEmitter } from '@angular/core';
import { SuiTabContent } from "./tab-content";
import { Observable } from "rxjs";
export declare class SuiTab {
    id: string;
    suiTabHeader: string;
    private _content;
    content: SuiTabContent;
    stateChanged$: Observable<SuiTab>;
    private _stateObserver;
    constructor();
    private _isActive;
    private _isDisabled;
    isActive: boolean;
    isDisabled: boolean;
    private stateObserverNext(change);
    manuallyActivate: boolean;
    manuallyDisable: boolean;
    isActiveChange: EventEmitter<boolean>;
    onActivate: EventEmitter<SuiTab>;
    private click();
}
