import { EventEmitter } from '@angular/core';
export declare class SuiMessage {
    dismissible: boolean;
    onDismiss: EventEmitter<SuiMessage>;
    private dismissed;
    private dismiss();
    private classes;
}
export declare const SUI_MESSAGE_DIRECTIVES: typeof SuiMessage[];
